package simple.outliner.builder.math.second.geom;

/** Section type. */
public enum SegmentType
{
    /** Type use if segment was created from Line. */
    HARD,
    /** Type use if segment was created in order to join to points in polygon but not represents line. */
    SOFT
}
